# Production Deployment Guide\n\n## 🚀 CD Pipeline Setup Complete\n\nYour CI/CD pipeline now includes automated deployment to production.\n\n### 📋 Required GitHub Secrets\n\nAdd these secrets in GitHub Settings > Secrets and variables > Actions:\n\n```bash\n# Docker Hub Credentials\nDOCKER_USERNAME=your_docker_username\nDOCKER_PASSWORD=your_docker_password\n\n# Production Server Access\nPRODUCTION_HOST=your.server.ip.address\nPRODUCTION_USER=ubuntu\nPRODUCTION_SSH_KEY=your_private_ssh_key\n```\n\n### 🖥️ Server Setup\n\n**1. Prepare Production Server:**\n```bash\n# Install Docker and Docker Compose\nsudo apt update\nsudo apt install docker.io docker-compose\nsudo usermod -aG docker $USER\n\n# Create application directory\nsudo mkdir -p /opt/school-support\nsudo chown $USER:$USER /opt/school-support\ncd /opt/school-support\n```\n\n**2. Upload Configuration Files:**\n```bash\n# Copy these files to /opt/school-support/\n- docker-compose.prod.yml → docker-compose.yml\n- .env.prod.example → .env (with actual values)\n- nginx.conf (if using Nginx)\n```\n\n**3. Environment Variables:**\n```bash\n# Create .env file with production values\ncp .env.prod.example .env\n# Edit .env with your actual production values\n```\n\n### 🔄 Deployment Flow\n\n**Automatic Deployment Triggers:**\n- ✅ Push to `main` branch\n- ✅ All tests pass\n- ✅ Code quality checks pass\n- ✅ Docker image builds successfully\n\n**Deployment Steps:**\n1. **Build**: Creates Docker image with commit SHA tag\n2. **Push**: Uploads to Docker Hub\n3. **Deploy**: SSH to server and updates containers\n4. **Cleanup**: Removes old Docker images\n\n### 🛡️ Production Environment\n\nThe pipeline creates a `production` environment in GitHub with:\n- Manual approval requirement (optional)\n- Environment-specific secrets\n- Deployment protection rules\n\n### 📊 Monitoring\n\n**Deployment Status:**\n- View in GitHub Actions tab\n- Check Docker Hub for new images\n- Monitor server logs: `docker-compose logs -f`\n\n**Health Checks:**\n```bash\n# Check container status\ndocker-compose ps\n\n# View application logs\ndocker-compose logs backend\n\n# Check database connection\ndocker-compose exec backend python manage.py check --database default\n```\n\n### 🔧 Manual Deployment\n\nIf needed, deploy manually:\n```bash\n# On production server\ncd /opt/school-support\ndocker pull your_username/school-support-backend:latest\ndocker-compose down\ndocker-compose up -d\n```\n\n### 🚨 Rollback Procedure\n\n```bash\n# Rollback to previous version\ndocker pull your_username/school-support-backend:previous_sha\ndocker tag your_username/school-support-backend:previous_sha your_username/school-support-backend:latest\ndocker-compose down\ndocker-compose up -d\n```\n\n### ✅ Verification\n\nAfter deployment:\n1. Check application health: `https://yourdomain.com/api/health/`\n2. Verify database connectivity\n3. Test user authentication\n4. Monitor error logs\n\nYour Django application now has enterprise-grade CI/CD with automated testing, building, and deployment!